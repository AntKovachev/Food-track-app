import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';

@NgModule({
  imports: [BrowserModule], // Import only the modules you need
  declarations: [],
  providers: [],
  bootstrap: []  // Leave this empty because AppComponent is standalone now
})
export class AppModule { }
